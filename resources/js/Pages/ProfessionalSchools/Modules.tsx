import StructurePage from '@/Components/Platform/StructurePage';
export default function Modules({professionalSchool, modules, programmes = [], courses = [], canManageStructure = true}: any) {
    return <StructurePage title="Modules" owner={professionalSchool.name} path={'/professional-schools/'+professionalSchool.id+'/modules'} rows={modules} canManage={canManageStructure} defaults={{ name:'', code:'', description:'', status:'active',programme_id:'',course_id:'' }} fields={[{name:'name',label:'Name',required:true},{name:'code',label:'Code'},{name:'description',label:'Description',type:'textarea'},{name:'programme_id',label:'Programme',required:false,options:programmes.map((r:any)=>({value:String(r.id),label:r.name}))},{name:'course_id',label:'Course',required:true,options:courses.map((r:any)=>({value:String(r.id),label:r.name}))}]} />;
}
